# Example environment variables for local development
# DO NOT COMMIT THIS FILE WITH REAL SECRETS!
#
# Usage:
#   1. Copy this file: cp .env.example .env
#   2. Replace REPLACE_ME values with your actual configuration
#   3. Load into shell: set -a; source .env; set +a
#   4. Or use with docker-compose or direnv
#
# The actual .env file is gitignored and will not be committed.

# Google Cloud Platform Configuration
PROJECT_ID=datawarehouse-422511
REGION=europe-west1
GCS_BUCKET=mmm-app-output

# Cloud Run Jobs Configuration
QUEUE_ROOT=robyn-queues
DEFAULT_QUEUE_NAME=default
SAFE_LAG_SECONDS_AFTER_RUNNING=5

# Snowflake Configuration
# These values are used by the app for Snowflake connectivity
SF_USER=REPLACE_ME
SF_ACCOUNT=REPLACE_ME
SF_WAREHOUSE=REPLACE_ME
SF_DATABASE=REPLACE_ME
SF_SCHEMA=REPLACE_ME
SF_ROLE=REPLACE_ME

# Snowflake Authentication - Private Key (for deployed environments)
# For key-pair authentication, set the secret name used in Secret Manager
SF_PRIVATE_KEY_SECRET=sf-private-key
SF_PERSISTENT_KEY_SECRET=sf-private-key-persistent

# Snowflake Authentication - Password (optional, for local dev only)
# SF_PASSWORD=REPLACE_ME_IF_USING_PASSWORD_AUTH
